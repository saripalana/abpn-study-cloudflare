{
  "name": "abpn-study-cloudflare",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "app:compat": "node scripts/patch-multiselect-app.mjs && node scripts/patch-multi-deck-builder-ui.mjs && node scripts/patch-multi-deck-session-app.mjs && node scripts/patch-multi-deck-runtime-app.mjs && node scripts/patch-multi-deck-results-correctness.mjs && node scripts/patch-user-facing-decks.mjs && node scripts/patch-cloud-deck-imports.mjs",
    "worker:compat": "node scripts/patch-deck-library-worker.mjs",
    "bank:import": "node scripts/import-ks-bank.mjs && npm run app:compat && npm run worker:compat",
    "dev": "npm run bank:import && wrangler dev",
    "dev:test": "npm run bank:import && wrangler dev --local --port 8787 --var APP_RELEASE_MODE:full --var CLOUD_SYNC_ENABLED:false --var ACCESS_JWT_REQUIRED:false",
    "deploy": "npm run verify && npm run db:migrate:remote && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply abpn-study-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply abpn-study-db --remote",
    "guardrails": "bash scripts/check-cost-guardrails.sh",
    "test": "npm run app:compat && npm run worker:compat && node --test",
    "test:e2e": "playwright test",
    "verify": "npm run bank:import && npm run guardrails && npm test && wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "6.2.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "wrangler": "^4.0.0"
  }
}
