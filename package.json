{
  "name": "abpn-study-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-browser-assets.mjs --write",
    "build:check": "node scripts/build-browser-assets.mjs --check",
    "build:idempotence": "node scripts/build-browser-assets.mjs --verify-idempotent",
    "bank:import": "node scripts/import-ks-bank.mjs && npm run build",
    "dev": "npm run bank:import && wrangler dev",
    "dev:test": "npm run bank:import && wrangler dev --local --port 8787 --var APP_RELEASE_MODE:full --var CLOUD_SYNC_ENABLED:false --var ACCESS_JWT_REQUIRED:false",
    "staging:check": "npm run verify && wrangler deploy --dry-run --config wrangler.staging.toml",
    "deploy": "npm run verify && npm run db:migrate:remote && wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply abpn-study-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply abpn-study-db --remote",
    "guardrails": "bash scripts/check-cost-guardrails.sh",
    "test": "npm run build:check && node --test",
    "test:e2e": "playwright test",
    "verify": "npm run bank:import && npm run build:idempotence && npm run guardrails && npm test && wrangler deploy --dry-run"
  },
  "dependencies": {
    "jose": "6.2.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
